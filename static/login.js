const form=document.querySelector('#login-form'),errorBox=document.querySelector('#login-error'),submitButton=form.querySelector('button');
form.addEventListener('submit',async event=>{event.preventDefault();errorBox.hidden=true;submitButton.disabled=true;const credentials={username:form.username.value.trim(),password:form.password.value};try{const response=await fetch('/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(credentials)});const data=await response.json();if(!response.ok)throw Error(data.error||'No fue posible iniciar sesión.');location.assign('/')}catch(error){errorBox.textContent=error.message;errorBox.hidden=false;submitButton.disabled=false;form.password.value='';form.password.focus();}});
